public class logicOperators {
    public static void main(String[] args) {
        // == => used to compare equal values
        // != => used to compare values not equal 
        // > => Greater than
        // < => Less than
        // >= Greater or equal too
        // <= Less or equal too

        // && => Return true if both statements are equal
        // || => Return true if one of the statements is true
        // ! => Reverses the result, returns false if the result is true
    }
}
